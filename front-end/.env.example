# Use caminho relativo (/api):
# - Dev: proxy do Vite -> backend
# - Docker/produção: proxy do Nginx -> project_vet_back_end
# Evita lentidão do localhost no Windows (IPv6 ::1 vs IPv4 127.0.0.1).
VITE_API_URL=/api
